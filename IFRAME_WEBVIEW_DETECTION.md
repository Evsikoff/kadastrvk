# 🎮 Определение iframe/WebView и автоматическая адаптация

## Описание

Приложение теперь автоматически определяет контекст своего запуска (iframe, webView или обычный браузер) и подстраивает размеры игрового экрана под контейнер на раннем этапе загрузки.

## Возможности

### 1. Автоматическое определение контекста

Приложение определяет следующие контексты:

- **iframe** - когда приложение встроено в другую веб-страницу через `<iframe>`
- **WebView** - когда приложение открыто в мобильном приложении (VK, Telegram, Instagram и др.)
- **Browser** - когда приложение открыто в обычном браузере

### 2. Автоматическая адаптация размеров

При открытии в iframe или webView:
- Определяется размер контейнера (viewport)
- Вычисляется соотношение сторон контейнера
- Игровой экран автоматически подстраивается под оптимальные размеры
- Поддерживаются различные ориентации (портрет, ландшафт, квадрат)

### 3. Динамическое изменение размеров

При изменении размера контейнера игра автоматически адаптируется:
- Изменение размера окна браузера
- Поворот устройства
- Изменение размера iframe родительской страницей

## Технические детали

### Утилита ContainerDetector

Новый класс `ContainerDetector` (файл: `src/utils/ContainerDetector.js`) предоставляет методы для:

#### Определение контекста
```javascript
// Проверка, открыто ли в iframe
ContainerDetector.isInIframe() // boolean

// Проверка, открыто ли в webView
ContainerDetector.isInWebView() // boolean

// Проверка, открыто ли во встроенном контейнере
ContainerDetector.isEmbedded() // boolean

// Получение полной информации о контейнере
ContainerDetector.getContainerInfo()
// { isEmbedded, isIframe, isWebView, containerType }
```

#### Определение размеров
```javascript
// Получение размеров контейнера
ContainerDetector.getContainerSize()
// { width, height }

// Получение соотношения сторон
ContainerDetector.getAspectRatio()
// number (width / height)

// Получение оптимальных размеров игры
ContainerDetector.getOptimalGameSize({
  baseWidth: 1920,
  minAspectRatio: 0.5,  // портрет 9:16
  maxAspectRatio: 2.0   // ландшафт 16:9
})
// { width, height, aspectRatio, containerWidth, containerHeight }
```

#### Отладка
```javascript
// Вывод информации в консоль
ContainerDetector.logInfo()
```

### Интеграция в main.js

Функция `getGameSize()` теперь:

1. Проверяет контекст запуска через `ContainerDetector.getContainerInfo()`
2. Если приложение встроено (iframe/webView):
   - Использует `ContainerDetector.getOptimalGameSize()` для определения размеров
   - Логирует информацию о контейнере
3. Для обычного браузера использует стандартную логику

### Поддерживаемые соотношения сторон

По умолчанию поддерживаются:
- **Минимум**: 0.5 (портретная ориентация 9:16)
- **Максимум**: 2.0 (широкая ландшафтная ориентация)
- **Базовая ширина**: 1920px

Эти параметры можно настроить при вызове `getOptimalGameSize()`.

## Использование

### Встраивание в iframe

```html
<!DOCTYPE html>
<html>
<head>
  <title>Встроенная игра</title>
  <style>
    iframe {
      width: 800px;
      height: 600px;
      border: none;
    }
  </style>
</head>
<body>
  <iframe src="https://your-game-url.com"></iframe>
</body>
</html>
```

Игра автоматически определит, что она в iframe, и подстроится под размер 800x600.

### Тестирование

Для тестирования работы в iframe используйте файл `iframe-test.html`:

```bash
npm run dev
# Откройте http://localhost:5173/iframe-test.html
```

Тестовая страница позволяет:
- Переключать между различными размерами iframe
- Устанавливать пользовательские размеры
- Наблюдать за автоматической адаптацией игры

### Отладка

Для включения подробного логирования добавьте параметр `debug` в URL:

```
http://localhost:5173/?debug
```

При этом в консоль будет выведена информация:
- Тип контейнера (iframe/webView/browser)
- Размеры контейнера
- Соотношение сторон
- Оптимальные размеры игры

Логирование также включается автоматически при обнаружении встроенного контейнера.

## Поддерживаемые WebView

Утилита определяет следующие типы WebView:

- iOS WebView
- Android WebView
- VK WebView
- Telegram WebView
- Facebook WebView
- Instagram WebView
- Twitter WebView

## Примеры соотношений сторон

| Ориентация | Соотношение | Размеры (пример) | Game Size |
|------------|-------------|------------------|-----------|
| Портрет (мобильный) | 9:16 (0.56) | 360x640 | 1080x1920 |
| Квадрат | 1:1 | 600x600 | 1920x1920 |
| Ландшафт | 4:3 (1.33) | 800x600 | 1920x1440 |
| Ландшафт | 16:9 (1.78) | 960x540 | 1920x1080 |
| Ультраширокий | 21:9 (2.33)* | 1200x500 | 1920x800 |

*Ограничено максимумом 2.0, итоговое соотношение будет 2.0

## Обратная совместимость

Изменения полностью обратно совместимы:
- Для обычных браузеров поведение не изменилось
- Мобильные устройства продолжают использовать стандартную логику
- Новая функциональность активируется только при обнаружении iframe/webView

## Производительность

- Определение контекста происходит один раз при запуске
- Вычисления минимальны и не влияют на производительность
- Изменение размеров использует debounce для iframe (100ms)

## Файлы

Добавленные/измененные файлы:
- `src/utils/ContainerDetector.js` - новая утилита для определения контекста
- `src/main.js` - интеграция определения контекста
- `iframe-test.html` - тестовая страница для проверки работы в iframe
- `IFRAME_WEBVIEW_DETECTION.md` - данная документация
